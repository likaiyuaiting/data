wp=800*2*pi; 
ws=600*2*pi; 
rp=0.1;
rs=60; 
[N,wpo]=ellipord(wp,ws,rp,rs,'s'); 
[B,A]=ellip(N,rp,rs,wpo,'high','s'); 
fk=linspace(0,1000,200);        %等分段 
w=2*pi*fk;
h=freqs(B,A,w);  
magh=abs(h) ;
plot(fk,magh); 
title('高通幅频响应'); 
